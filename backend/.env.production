# EquipTracker — Producción
# Copiar a .env y ajustar valores

POSTGRES_DB=equiptracker
POSTGRES_USER=equiptracker
POSTGRES_PASSWORD=<generar-contraseña-segura>
SECRET_KEY=<generar-clave-segura>

# Para desarrollo local con SQLite (comentar lo de arriba):
# DATABASE_URL=sqlite:///./equip_tracker.db
# SECRET_KEY=change-me-in-production
